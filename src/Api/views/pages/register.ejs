<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Registrarse</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script
      src="https://cdnjs.cloudflare.com/ajax/libs/axios/0.27.2/axios.min.js"
      integrity="sha512-odNmoc1XJy5x1TMVMdC7EMs3IVdItLPlCeL5vSUPN2llYKMJ2eByTTAIiiuqLg+GdNr9hF6z81p27DArRFKT7A=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    ></script>
  </head>
  <body>
    <main class="min-h-screen flex items-center justify-center bg-gray-100">
      <div
        class="w-full max-w-md space-y-5 py-14 px-8 bg-white shadow-xl rounded-lg"
      >
        <div>
          <h1 class="text-center text-2xl font-bold mb-10 text-gray-700">
            🏷 STREET WEAR
          </h1>
          <h2 class="text-2xl font-medium text-gray-700">Bienvenido! 👋🏻</h2>
          <p class="text-gray-500 mt-2">
            Registrate para comprar más rápido y llevar el control de tus
            pedidos ¡en un solo lugar!
          </p>
        </div>

        <!-- <form
          class=""
          action="/api/auth/signUp"
          method="POST"
          enctype="multipart/form-data"
        > -->
        <form onsubmit="SignUp(event)">
          <input
            type="text"
            id="name"
            name="name"
            placeholder="Nombre y Apellido *"
            class="w-full p-3 mb-5 rounded-md border-2 border-gray-200 transition-all focus:outline-0 focus:border-green-400 focus:border-2"
            required
          />
          <input
            type="text"
            id="province"
            name="province"
            placeholder="Provincia *"
            class="w-full p-3 mb-5 rounded-md border-2 border-gray-200 transition-all focus:outline-0 focus:border-green-400 focus:border-2"
            required
          />
          <input
            type="text"
            id="address"
            name="address"
            placeholder="Domicilio *"
            class="w-full p-3 mb-5 rounded-md border-2 border-gray-200 transition-all focus:outline-0 focus:border-green-400 focus:border-2"
            required
          />
          <input
            type="number"
            min="0"
            max="99999"
            id="postalCode"
            name="postalCode"
            placeholder="Cód. Postal *"
            class="w-full p-3 mb-5 rounded-md border-2 border-gray-200 transition-all focus:outline-0 focus:border-green-400 focus:border-2"
            required
          />
          <input
            type="text"
            id="phone"
            name="phone"
            placeholder="Nº Teléfono *"
            class="w-full p-3 mb-5 rounded-md border-2 border-gray-200 transition-all focus:outline-0 focus:border-green-400 focus:border-2"
            required
          />
          <input
            type="email"
            id="email"
            name="email"
            placeholder="Email *"
            class="w-full p-3 mb-5 rounded-md border-2 border-gray-200 transition-all focus:outline-0 focus:border-green-400 focus:border-2"
            required
          />
          <input
            type="password"
            id="password"
            name="password"
            placeholder="Contraseña *"
            class="w-full p-3 mb-2 rounded-md border-2 border-gray-200 transition-all focus:outline-0 focus:border-green-400 focus:border-2"
            required
          />
          <input
            type="file"
            id="avatar"
            name="avatar"
            class="w-full p-3 mb-3 mt-3 rounded-md border-2 border-gray-200 transition-all focus:outline-0 focus:border-green-400 focus:border-2"
            required
          />
          <p class="text-right mb-10 text-md">
            ¿Ya tienes cuenta?
            <a href="IniciarSesion" class="font-bold">Ingresa aquí</a>
          </p>

          <div role="alert" class="hidden mb-5" id="errorMessageBox">
            <div class="bg-red-500 text-white font-bold rounded-t px-4 py-2">
              Error
            </div>
            <div
              class="border border-t-0 border-red-400 rounded-b bg-red-100 px-4 py-3 text-red-700"
            >
              <p id="errorMessage"></p>
            </div>
          </div>

          <button
            type="submit"
            class="py-2 px-3 w-full bg-green-700 rounded-md shadow-md text-white tracking-wide font-bold transition-all hover:shadow-lg hover:bg-green-500"
          >
            REGISTRARME
          </button>
        </form>
      </div>
    </main>

    <script>
      const errorMessage = document.getElementById('errorMessage');
      const errorMessageBox = document.getElementById('errorMessageBox');

      function SignUp(event) {
        event.preventDefault();

        const {
          name,
          province,
          address,
          postalCode,
          phone,
          email,
          password,
          avatar,
        } = event.target.elements;

        let formData = new FormData();
        formData.append('name', name.value);
        formData.append('province', province.value);
        formData.append('postalCode', parseInt(postalCode.value));
        formData.append('address', address.value);
        formData.append('phone', phone.value);
        formData.append('email', email.value);
        formData.append('password', password.value);
        formData.append('avatar', avatar.files[0]);

        axios({
          method: 'POST',
          headers: { 'Content-Type': 'multipart/form-data' },
          data: formData,
          withCredentials: true,
          url: `http://localhost:8080/api/auth/signUp`,
        })
          .then((resp) => {
            if (resp.request?.responseURL.includes('success')) {
              window.location.href = '/';
            } else if (resp.request?.responseURL.includes('error')) {
              errorMessage.innerHTML = 'Verifique los datos ingresados.';
            }
          })
          .catch((error) => {
            if (error.request?.responseURL.includes('success')) {
              window.location.href = '/';
            } else if (error.request?.responseURL.includes('error')) {
              errorMessageBox.classList.toggle('hidden');
              errorMessage.innerHTML = 'Verifique los datos ingresados.';
            } else {
              errorMessageBox.classList.toggle('hidden');
              errorMessage.innerHTML =
                'Algo salió mal al intentar Registrarlo en nuestro sistema. Por favor, intenta nuevamente';
            }

            if (!error.response.data.success) {
              errorMessage.innerHTML = error.response.data.message;
            }
          });
      }
    </script>
  </body>
</html>

## 🐶 PUG

Go to the project folder whit:

```
cd pug
```

Run the server whit:

```
npx nodemon server.js
```

---

## 🧔🏻 HANDLEBARS

Go to the project folder whit:

```
cd handlebars
```

Run the server whit:

```
npx nodemon server.js
```
---

## <% EJS

Go to the project folder whit:

```
cd ejs
```

Run the server whit:

```
npx nodemon server.js
```